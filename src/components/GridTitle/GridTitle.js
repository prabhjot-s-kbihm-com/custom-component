import React from 'react';

const GridTitle = (props) => {

  return (<h2 style={{backgroundColor: '#00bfff', padding: '5px 30px', color:'#ffffff'}}>
      {props.title}
  </h2>)
}

export default GridTitle;